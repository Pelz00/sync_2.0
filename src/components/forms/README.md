# components/forms

Thin wrappers over `react-hook-form` that wire up zod errors and accessible labels.

- `FormField` - label + control + error in a consistent layout.
- `FormError` - surfaces zod issues with the right ARIA wiring.
- `FormSection` - grouped fields with an eyebrow + description.
