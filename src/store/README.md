# store

Redux Toolkit store. **Client-only UI state**: cart, multi-step wizard progress, filter selections, open/closed UI panels.

Do NOT put server data here. Server data is fetched in RSCs or mutated via Server Actions.
